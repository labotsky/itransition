.post-home
  %h2=paragraph.name
  %p=paragraph.body.html_safe
  %p           
    =link_to t('button.view'), poem_paragraph_path(@poem, paragraph), class: "btn btn-small"          
    -if user_signed_in? && @poem.user == current_user        
      =link_to edit_poem_paragraph_path(@poem, paragraph), class: "btn btn-small btn-info" do
        %i.icon-edit
        =t('button.edit')
      =link_to poem_paragraph_path(@poem, paragraph), method: :delete, confirm: 'You sure?', class:"btn btn-small btn-danger" do
        %i.icon-trash
        =t('button.delete')