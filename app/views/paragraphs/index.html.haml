%h1 Listing paragraphs

%table
  %tr
    %th Poem
    %th Name
    %th Body
    %th
    %th
    %th

  - @paragraphs.each do |paragraph|
    %tr
      %td= paragraph.poem_id
      %td= paragraph.name
      %td= paragraph.body
      %td= link_to 'Show', paragraph
      %td= link_to 'Edit', edit_paragraph_path(paragraph)
      %td= link_to 'Destroy', paragraph, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Paragraph', new_paragraph_path
